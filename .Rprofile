# Turn on the markdown extensions
require(markdown)
options(markdown.extensions=markdownExtensions())

# Use pandoc to process markdown files.
# options(rstudio.markdownToHTML = 
#   function(inputFile, outputFile) {      
#     system(paste("pandoc", inputFile, "--mathml --number-sections --self-contained --smart -o", outputFile))
#   }
# )  
